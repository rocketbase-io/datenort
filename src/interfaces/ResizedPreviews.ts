export interface ResizedPreviews {
    previews: {
        "xs":   string;
        "s":    string;
        "m":    string;
        "l":    string;
        "xl":   string;
    }
}

export enum ResizeSize {
    xs = "xs",
    s = "s",
    m = "m",
    l = "l",
    xl = "xl"
}
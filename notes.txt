- auditor - subject aus dem JWT token 
- redirect for specific size xs, s, m, l, xl

- react umstellen auf datenort (endpoint)

- filepond-preconfigured.tsx (upload)

- pin/thread/create.tsx (UpdateAssetDisplay onChange => ) - FilePond in action

- interfaces ändern

- RocketImage component 
   - abändern auf datenort api

- asset-link (type)

- form-fields 
- bei type - error, auskommentieren oder löschen
